# Configuration pour développement local
# Copier ce fichier en .env.local et décommenter la ligne suivante pour utiliser le backend local

# VITE_API_BASE_URL=http://localhost:8000

# Pour utiliser le backend Render (par défaut, pas besoin de configuration)
# Le backend Render est déjà configuré dans environment.ts
