# Configuration pour développement local
# Copier ce fichier en .env.local et remplir les valeurs

# Clé API Groq (gratuite) pour le parsing intelligent de requêtes
# Obtenez votre clé sur: https://console.groq.com/keys
VITE_GROQ_API_KEY=your-groq-api-key-here

# Backend local (optionnel, décommenter pour utiliser localhost au lieu de Render)
# VITE_API_BASE_URL=http://localhost:8000

# Pour utiliser le backend Render (par défaut, pas besoin de configuration)
# Le backend Render est déjà configuré dans environment.ts
